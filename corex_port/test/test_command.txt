# On-GPU test for the Tengine CUDA backend (device = GPU 1 / CUDA_VISIBLE_DEVICES=1).
# Runs the CUDA classification example on GPU for every bundled ImageNet-style
# benchmark model and asserts CUDA/CPU top-5 parity. Full output -> test/test.log.
source corex_port/env/corex_env.sh
bash corex_port/test/run_tests.sh > corex_port/test/test.log 2>&1

# Single-model manual invocation (what the harness runs per case):
./build/examples/tm_classification_cuda \
    -m benchmark/models/mobilenet_benchmark.tmfile \
    -i /tmp/test_img.jpg -g 224,224 \
    -s 0.017,0.017,0.017 -w 104.007,116.669,122.679 -r 3
